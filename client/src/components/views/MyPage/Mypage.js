import React from 'react';
import { withRouter } from 'react-router-dom';
function Mypage() {
	//1. 프로필 사진이 등록되어 있다면= 그 사진을 배경으로 위에 컨테이너 생성
	//2. 사용자 정보들 불러오기
	//3. 수정하기 버튼 생성- 컴포넌트

	return <div></div>;
}

export default withRouter(Mypage);
